extends MeshInstance3D

	
# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	#if path_3d.curve.point_count < Globals.celestial_body_path[0].size():
	#	update_path()
	#if path_3d.curve.point_count == Globals.celestial_body_path[0].size():
#		update_path()
	pass
